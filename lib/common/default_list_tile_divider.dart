import 'package:flutter/material.dart';

class DefaultListTileDivider extends StatelessWidget {
  const DefaultListTileDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(color: Colors.blueGrey,thickness: 0.8);
  }
}